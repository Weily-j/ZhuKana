{
  "name": "zhuyin-jp-keyboard-pwa",
  "private": true,
  "type": "module",
  "scripts": {
    "build:pages": "node ./scripts/build-pages.mjs",
    "test": "node --test",
    "generate:candidates": "node ./scripts/generate-candidates.mjs"
  }
}
